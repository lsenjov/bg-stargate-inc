{
  "name": "@stargate-inc/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "predev": "npm run build --workspace=@stargate-inc/shared",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --build --pretty false"
  },
  "dependencies": {
    "@stargate-inc/shared": "*",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3",
    "tsx": "^4.20.3"
  }
}
