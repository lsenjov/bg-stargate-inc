# Modules and factories rules

1. Document modules, factories, production scaling, module acquisition, construction, and the starting module roster in the human-facing rules and component references. Record unresolved interpretations as open questions. Commit the documentation.
2. Update the shared feature manifest and its tests so the in-app rulebook marks the newly defined module/factory economy as planned rather than playable. Commit the manifest change.
3. Review both commits for rule fidelity, contradictions, and test coverage. Fix and re-review until no high- or medium-severity issues remain.
