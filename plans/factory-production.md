# Factory production

1. Add the shared economy, module catalog, held-module inventory, factory state, and construction operations. Players begin with $20, the exact resources needed to construct all six starting modules, and those six modules held. Construction pays costs, creates permanent ordered factories or extends matching factories, and supports the three fixed exoplanet slots. Add engine tests for costs, type matching, slot capacity, placement order, ownership, held modules, and public state. Commit the shared construction model and tests.
2. Add server-authoritative connection reward and production operations. The normal first connection round starts immediately. After a successful self/exoplanet connection, the connected player may construct held modules and then run factories. They choose factories in any order for successive running-cost multipliers, resolve modules oldest-first one at a time, receive outputs immediately, may stop a factory or production, and may operate every module at a connected exoplanet regardless of ownership. Add engine tests for affordability, unchanged inputs, output chaining, stopping, multiplier assignment, ownership, and phase/round gating. Commit the production engine and tests.
3. Add validated Socket.IO commands for starting-module installation and production, keep selection deadlines inactive during setup/production, and prevent the next round until every eligible player finishes production. Add protocol and server integration coverage. Commit the multiplayer transport changes.
4. Add responsive client views for arranging starting factories, inspecting public factories and balances, and operating factories after a self or exoplanet connection. Cover setup choices, live production state, errors, and completion in UI tests. Commit the client experience.
5. Update the feature manifest, README, and human-facing HTML rules/component/design records to match the shipped behavior and resolved decisions while leaving empty exoplanet acquisition and wider construction marked planned. Run the full repository check and commit the rule/status updates.
6. Have a fresh agent review the implementation for correctness, security, accessibility, rule fidelity, and test gaps. Fix and commit all high- or medium-severity findings, then repeat review until none remain. Report any low-severity findings before further work.
